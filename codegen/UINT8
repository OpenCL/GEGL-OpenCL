DATATYPE_BEGIN
DATATYPE		guint8
WP			255
WP_NORM			(1/255.0)
MIN_CHAN		0
MAX_CHAN		255	
ZERO			0
CHAN_CLAMP (i)		CLAMP (i,0,255)
WP_CLAMP (i)		CLAMP (i,0,255)
CHAN_MULT (a,b)		((t) = (a) * (b) + 0x80, ((((t) >> 8) + (t)) >> 8)
ROUND (i)		ROUND (i)
DATATYPE_END
